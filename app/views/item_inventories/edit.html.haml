= render 'layouts/header'

%h2 editインベントリー
= form_for [current_user, @item, @itemInventory]  do |f|
  .field
    = f.label "残り"
    %br/
    = f.number_field :amount
    ％
  .actions
    = f.submit "更新!"