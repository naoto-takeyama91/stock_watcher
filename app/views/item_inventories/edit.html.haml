= render 'layouts/header'

%h2 editインベントリー
= form_for [@item, @itemInventory], url: user_item_item_inventory_path do |f|
  .field
    = f.label "残り"
    %br/
    = f.number_field :amount
    ％
  .actions
    = f.submit "更新!"