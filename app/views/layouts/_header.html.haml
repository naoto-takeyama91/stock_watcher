%header.header
  %h1.header__title
    = link_to root_path, class: 'title' do
      stock watcher
  %nav.header__nav-list
    %ul.lists
      %li.user-name
        = current_user.name
      %li
        = link_to edit_user_path(current_user), class: 'menu' do
          アカウント編集
      %li
        = link_to user_purchase_histories_path(current_user), class: 'menu' do
          購入履歴
      %li
        = link_to destroy_user_session_path, method: :delete, class: 'menu' do
          ログアウト