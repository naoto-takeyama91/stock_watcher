%header.header
  %h1.header__title
    = link_to root_path, class: 'title' do
      stock watcher
  %nav.header__nav-list
    %ul.lists
      %li.user-name
        = current_user.name
      %li
        = link_to edit_user_registration_path(current_user), class: 'menu' do
          アカウント編集
      %li
        = link_to '#', class: 'menu' do
          購入履歴
      %li
        = link_to '#', class: 'menu' do
          設定
      %li
        = link_to destroy_user_session_path, method: 'delete', class: 'menu' do
          ログアウト